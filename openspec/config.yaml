# OpenSpec config — futbol-carrer (auto-generated by sdd-init, 2026-08-04)
schema: spec-driven

context: |
  App: Fútbol Carrera (slug football-career) — footballer career simulator, offline-first.
  Stack: Expo SDK 57, React Native 0.86.2, React 19.2.3, TypeScript ~6.0.3 (strict).
  State: Zustand 5 + expo-sqlite (local-first); expo-router file-based routing (typed routes, React Compiler on).
  Architecture: Clean/Hexagonal in src/ (domain, data, presentation, services, state, shared); atomic design (atoms/molecules/organisms).
  Path alias: @/* -> ./src/*. Conventions: read Expo v57 docs before coding (AGENTS.md); Prettier + ESLint flat (expo lint).
  Testing: no runner configured; strict_tdd disabled.

rules:
  proposal:
    - Include rollback plan for risky changes
  specs:
    - Use Given/When/Then for scenarios
    - Use RFC 2119 keywords (MUST, SHALL, SHOULD, MAY)
  design:
    - Include sequence diagrams for complex flows
    - Document architecture decisions with rationale
    - Verify Expo SDK 57 APIs against https://docs.expo.dev/versions/v57.0.0/ before proposing code
  tasks:
    - Group by phase, use hierarchical numbering
    - Keep tasks completable in one session
  apply:
    guidelines:
      - Follow existing Clean/Hexagonal and atomic design patterns in src/
      - Do not touch uncommitted user work (identity.tsx, seed-clubes.ts, game.ts)
    tdd: false
    test_command: ""
  verify:
    test_command: ""
    build_command: "npx tsc --noEmit"
    coverage_threshold: 0
  archive:
    - Warn before merging destructive deltas
